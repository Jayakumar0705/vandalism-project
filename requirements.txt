# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Vandalism Detection Using Surveillance Camera
# Python Dependencies
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# ── Frontend ──
streamlit>=1.28.0

# ── Computer Vision ──
opencv-python-headless>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0

# ── Deep Learning (Feature Extraction for offline fallback) ──
tensorflow>=2.13.0
scikit-learn>=1.3.0

# ── AI Vision & Report Generation ──
google-genai>=1.0.0

# ── Environment / Config ──
python-dotenv>=1.0.0

# ── Testing ──
pytest>=7.4.0
